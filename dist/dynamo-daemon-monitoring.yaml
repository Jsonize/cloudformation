AWSTemplateFormatVersion: 2010-09-09

# https://s3.amazonaws.com/cloudformation.jsonize.com/dynamo-daemon-monitoring.yaml

Description: Monitor daemons via DynamoDB and CloudWatch Alarms


Parameters:

  DynamoDataTable:
    Type: String
    Description: The name of the monitoring data table
    Default: daemon-monitoring-records

  DynamoConfigTable:
    Type: String
    Description: The name of the monitoring config table
    Default: daemon-monitoring-config

  AlarmSNSTopic:
    Type: String
    Description: The SNS topic ARN that should be used for alarms

  ScheduleExpression:
    Type: String
    Description: Schedule expression, e.g. 'rate(6 hours)'
    Default: rate(15 minutes)


Resources:

  LambdaRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - lambda.amazonaws.com
            Action:
              - sts:AssumeRole
      Path: "/"
      Policies:
        - PolicyName: logs
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - logs:*
                Resource:
                  - arn:aws:logs:*:*:*
        - PolicyName: dynamo
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:CreateTable
                  - dynamodb:GetItem
                  - dynamodb:Scan
                  - dynamodb:Query
                Resource:
                  - !Join [ "", [ "arn:aws:dynamodb:*:*:table/", !Ref DynamoDataTable ] ]
                  - !Join [ "", [ "arn:aws:dynamodb:*:*:table/", !Ref DynamoConfigTable ] ]
        - PolicyName: sns
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - sns:Publish
                Resource:
                  - !Ref AlarmSNSTopic

  LambdaFunction:
    Type: AWS::Lambda::Function
    DependsOn:
      - LambdaRole
    Properties:
      Code:
        ZipFile: |
          _A='date'
          import boto3,os
          from botocore.exceptions import ClientError
          from boto3.dynamodb.conditions import Key
          import re,datetime,json
          def parseDuration(s):B={'second':1,'minute':60,'hour':60*60,'day':24*60*60,'week':7*24*60*60,'month':30*24*60*60};A=re.compile('(\\d+)\\s+([a-z]+[^s])s?').match(s);return int(A.group(1))*B[A.group(2)]
          def parseLastInvocation(s):
          	A=re.compile('rate\\((.*)\\)').match(s)
          	if A!=None:return datetime.datetime.now()-datetime.timedelta(seconds=parseDuration(A.group(1)))
          	B=re.compile('cron\\((.*)\\)').match(s)
          	if B!=None:return None
          	return None
          def log(s):print s
          def getTable(db,name,hashKey,rangeKey=None):
          	G='AttributeType';F='S';E='KeyType';B=hashKey;A='AttributeName'
          	try:
          		C=[{A:B,E:'HASH'}];D=[{A:B,G:F}]
          		if rangeKey!=None:C.append({A:_A,E:'RANGE'});D.append({A:_A,G:F})
          		return db.create_table(TableName=name,KeySchema=C,AttributeDefinitions=D,BillingMode='PAY_PER_REQUEST')
          	except ClientError:return db.Table(name)
          def handler(event,context):
          	p='timeout';o='contexts';n='sortkey';m='enabled';l='~';k='LastEvaluatedKey';Z='ALARM ';Y='Items';X='invocation';W='context';V='success';U='state';P=': ';O='daemonName';N='default';K='pending';J=' ';H=context;Q=boto3.resource('dynamodb');a=boto3.client('sns');b=getTable(Q,os.environ['DYNAMO_DATA_TABLE'],O,_A);R=getTable(Q,os.environ['DYNAMO_CONFIG_TABLE'],O);c=os.environ['SNS_TOPIC'];I=R.scan();S=I[Y]
          	while k in I:I=R.scan(ExclusiveStartKey=I[k]);S.extend(I[Y])
          	for C in S:
          		if m in C and not C[m]:continue
          		log(C);F=C[O];d=C['minInvocations'];e=parseDuration(C['thresholdStart']);f=parseDuration(C['timeoutEnd']);T=parseLastInvocation(C['schedule']);g=b.query(KeyConditionExpression=Key(O).eq(F)&Key(_A).gte(T.isoformat()));B={}
          		if o in C:
          			for E in C[o]:B[E]={}
          		else:B[N]={}
          		L=g[Y]
          		for A in L:A[_A]=A[_A].split(l,1)[0];A[n]=A[_A]+l+A[U]
          		L.sort(key=lambda x:x[n])
          		for A in L:
          			if not N in B:
          				if not W in A:continue
          				if not A[W]in B:continue
          				H=B[A[W]]
          			else:H=B[N]
          			if not A[X]in H:H[A[X]]={V:0,p:0,K:None}
          			G=H[A[X]];M=datetime.datetime.strptime(A[_A],'%Y-%m-%dT%H:%M:%S')
          			if A[U]=='start':
          				if M<=T+datetime.timedelta(seconds=e):G[K]=M
          			if A[U]=='stop'and G[K]!=None:
          				if M<=G[K]+datetime.timedelta(seconds=f):G[V]+=1
          				else:G[p]+=1
          				G[K]=None
          		for (h,i) in B.items():
          			D=0
          			for (q,j) in i.items():D+=j[V]
          			B[h]=D
          		if not B:B[N]=0
          		for (E,D) in B.items():
          			log(F+J+E+P+str(D))
          			if D<d:log(Z+F+J+E+P+str(D));a.publish(TopicArn=c,Message=json.dumps({'AlarmName':F+J+E,'OldStateValue':'UNKNOWN','NewStateValue':'ALARM','NewStateReason':Z+F+J+E+P+str(D)}),Subject=Z+F+J+E+P+str(D))
      Environment:
        Variables:
          DYNAMO_DATA_TABLE: !Ref DynamoDataTable
          DYNAMO_CONFIG_TABLE: !Ref DynamoConfigTable
          SNS_TOPIC: !Ref AlarmSNSTopic
      Handler: index.handler
      Role: !GetAtt LambdaRole.Arn
      Runtime: python3.7
      Timeout: 60

  ScheduledLambdaEventRule:
    Type: AWS::Events::Rule
    Properties:
      ScheduleExpression: !Ref ScheduleExpression
      State: ENABLED
      Targets:
        - Id: IdScheduledLambda
          Arn: !GetAtt LambdaFunction.Arn

  ScheduledLambdaPermission:
    Type: AWS::Lambda::Permission
    DependsOn:
      - ScheduledLambdaEventRule
    Properties:
      FunctionName: !GetAtt LambdaFunction.Arn
      Action: lambda:InvokeFunction
      Principal: events.amazonaws.com
      SourceArn: !GetAtt ScheduledLambdaEventRule.Arn
