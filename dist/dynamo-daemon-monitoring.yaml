AWSTemplateFormatVersion: 2010-09-09

# https://s3.amazonaws.com/cloudformation.jsonize.com/dynamo-daemon-monitoring.yaml

Description: Monitor daemons via DynamoDB and CloudWatch Alarms


Parameters:

  DynamoDataTable:
    Type: String
    Description: The name of the monitoring data table
    Default: daemon-monitoring-records

  DynamoConfigTable:
    Type: String
    Description: The name of the monitoring config table
    Default: daemon-monitoring-config

  AlarmSNSTopic:
    Type: String
    Description: The SNS topic ARN that should be used for alarms

  ScheduleExpression:
    Type: String
    Description: Schedule expression, e.g. 'rate(6 hours)'
    Default: rate(15 minutes)


Resources:

  LambdaRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - lambda.amazonaws.com
            Action:
              - sts:AssumeRole
      Path: "/"
      Policies:
        - PolicyName: logs
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - logs:*
                Resource:
                  - arn:aws:logs:*:*:*
        - PolicyName: dynamo
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:CreateTable
                  - dynamodb:GetItem
                  - dynamodb:Scan
                  - dynamodb:Query
                Resource:
                  - !Join [ "", [ "arn:aws:dynamodb:*:*:table/", !Ref DynamoDataTable ] ]
                  - !Join [ "", [ "arn:aws:dynamodb:*:*:table/", !Ref DynamoConfigTable ] ]
        - PolicyName: sns
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - sns:Publish
                Resource:
                  - !Ref AlarmSNSTopic

  LambdaFunction:
    Type: AWS::Lambda::Function
    DependsOn:
      - LambdaRole
    Properties:
      Code:
        ZipFile: |
          _A='date'
          import boto3,os
          from botocore.exceptions import ClientError
          from boto3.dynamodb.conditions import Key
          import re,datetime,json
          def parseDuration(s):B={'second':1,'minute':60,'hour':60*60,'day':24*60*60,'week':7*24*60*60,'month':30*24*60*60};A=re.compile('(\\d+)\\s+([a-z]+[^s])s?').match(s);return int(A.group(1))*B[A.group(2)]
          def parseLastInvocation(s):
          	A=re.compile('rate\\((.*)\\)').match(s)
          	if A!=None:return datetime.datetime.now()-datetime.timedelta(seconds=parseDuration(A.group(1)))
          	B=re.compile('cron\\((.*)\\)').match(s)
          	if B!=None:return None
          	return None
          def log(s):print s
          def getTable(db,name,hashKey,rangeKey=None):
          	G='AttributeType';F='S';E='KeyType';B=hashKey;A='AttributeName'
          	try:
          		C=[{A:B,E:'HASH'}];D=[{A:B,G:F}]
          		if rangeKey!=None:C.append({A:_A,E:'RANGE'});D.append({A:_A,G:F})
          		return db.create_table(TableName=name,KeySchema=C,AttributeDefinitions=D,BillingMode='PAY_PER_REQUEST')
          	except ClientError:return db.Table(name)
          def handler(event,context):
          	j='Items';i='contexts';h='state';U='ALARM ';T='timeout';S='invocation';R='success';P=': ';O='daemonName';N='context';K=' ';J='default';I='pending';H=context;Q=boto3.resource('dynamodb');V=boto3.client('sns');W=getTable(Q,os.environ['DYNAMO_DATA_TABLE'],O,_A);X=getTable(Q,os.environ['DYNAMO_CONFIG_TABLE'],O);Y=os.environ['SNS_TOPIC'];Z=X.scan()
          	for C in Z[j]:
          		log(C);G=C[O];a=C['minInvocations'];b=parseDuration(C['thresholdStart']);c=parseDuration(C['timeoutEnd']);L=parseLastInvocation(C['schedule'])-datetime.timedelta(seconds=parseDuration(rate.group(1)));print L.isoformat();d=W.query(KeyConditionExpression=Key(O).eq(G)&Key(_A).gte(L.isoformat()));A={}
          		if C[i]:
          			for F in C[i]:A[F]={}
          		else:A[J]={}
          		for B in d[j]:
          			if N in B:
          				if not B[N]in A:A[B[N]]={}
          				H=A[B[N]]
          			else:
          				if not J in A:A[J]={}
          				H=A[J]
          			if not B[S]in H:H[B[S]]={R:0,T:0,I:None}
          			D=H[B[S]];M=datetime.datetime.strptime(B[_A].split('~',1)[0],'%Y-%m-%dT%H:%M:%S')
          			if B[h]=='start':
          				if D[I]==None:
          					if M<=L+datetime.timedelta(seconds=b):D[I]=M
          				else:D[T]+=1
          			if B[h]=='stop'and D[I]!=None:
          				if M<=D[I]+datetime.timedelta(seconds=c):D[R]+=1
          				else:D[T]+=1
          				D[I]=None
          		for (e,f) in A.items():
          			E=0
          			for (k,g) in f.items():E+=g[R]
          			A[e]=E
          		if not A:A[J]=0
          		for (F,E) in A.items():
          			log(G+K+F+P+str(E))
          			if E<a:log(U+G+K+F+P+str(E));V.publish(TopicArn=Y,Message=json.dumps({'AlarmName':G+K+F,'OldStateValue':'UNKNOWN','NewStateValue':'ALARM','NewStateReason':U+G+K+F+P+str(E)}),Subject=U+G+K+F+P+str(E))
      Environment:
        Variables:
          DYNAMO_DATA_TABLE: !Ref DynamoDataTable
          DYNAMO_CONFIG_TABLE: !Ref DynamoConfigTable
          SNS_TOPIC: !Ref AlarmSNSTopic
      Handler: index.handler
      Role: !GetAtt LambdaRole.Arn
      Runtime: python3.7
      Timeout: 60

  ScheduledLambdaEventRule:
    Type: AWS::Events::Rule
    Properties:
      ScheduleExpression: !Ref ScheduleExpression
      State: ENABLED
      Targets:
        - Id: IdScheduledLambda
          Arn: !GetAtt LambdaFunction.Arn

  ScheduledLambdaPermission:
    Type: AWS::Lambda::Permission
    DependsOn:
      - ScheduledLambdaEventRule
    Properties:
      FunctionName: !GetAtt LambdaFunction.Arn
      Action: lambda:InvokeFunction
      Principal: events.amazonaws.com
      SourceArn: !GetAtt ScheduledLambdaEventRule.Arn
